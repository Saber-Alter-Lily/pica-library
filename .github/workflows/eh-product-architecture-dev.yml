name: Validate E-H product architecture consolidation

on:
  push:
    branches:
      - feature/eh-provider-v1
    paths:
      - 'scripts/dev/apply-product-source-architecture.mjs'
      - '.github/workflows/eh-product-architecture-dev.yml'

permissions:
  contents: write

concurrency:
  group: eh-product-architecture-dev
  cancel-in-progress: true

jobs:
  apply-and-validate:
    if: github.repository == 'Saber-Alter-Lily/pica-library'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/eh-provider-v1
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: pnpm/action-setup@v4
        with:
          version: '11.16.0'
      - run: pnpm install --frozen-lockfile
      - name: Apply product architecture patch
        run: node scripts/dev/apply-product-source-architecture.mjs
      - name: Validate Web and Desktop
        run: |
          pnpm type:check
          pnpm web:check
          pnpm test
          pnpm build
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - run: yes | sdkmanager 'platforms;android-35' 'build-tools;35.0.0'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Validate Android
        working-directory: mobile/android-alpha2
        run: gradle :app:testDebugUnitTest :app:lintDebug :app:assembleRelease --stacktrace
      - name: Commit validated architecture changes
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src web mobile/android-alpha2/app/src/main mobile/android-alpha2/app/src/test
          git diff --cached --check
          test -n "$(git diff --cached --name-only)"
          git commit -m 'feat: consolidate online sources accounts and library presentation'
          git push origin HEAD:feature/eh-provider-v1
