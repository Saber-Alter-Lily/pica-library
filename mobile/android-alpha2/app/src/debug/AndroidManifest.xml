<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Debug-only host entry used by P2-G15. Release manifests never contain this Activity. -->
        <activity
            android:name=".WorkerForceStopSeedActivity"
            android:exported="true" />
        <!-- Debug-only P2-G16 probes for trim-memory and Doze/background behavior. -->
        <activity
            android:name=".MemoryPressureProbeActivity"
            android:exported="true" />
        <activity
            android:name=".BackgroundRestrictionSeedActivity"
            android:exported="true" />
    </application>
</manifest>
