# Pica Library v0.1.4-dev.2 本地验收指南

这是本地 Windows 验收包，不是公开发布版。请使用合成测试数据或你明确选择的单个章节；不要把真实数据库复制给开发者。

## TEST 1 - 同步计数核对

同步前记录“漫画库”和“收藏”。点击“更新收藏”，记录收藏、新增收藏、取消收藏、漫画库新增和漫画库更新。若漫画库明显上升，请保存同步结果和截图；不要为了让两者相等而删除本地记录。

## TEST 2 - 网格尺寸

在漫画库和“为你推荐”分别测试“大 / 中 / 小”，再切换网格和列表。用半屏窗口重复一次，确认卡片自然换行。

## TEST 3 - 响应式控件

在 1600、1280、900、700 和手机宽度观察首页、漫画库、推荐、下载任务、已下载、设置 / Browser Lite。确认没有横向滚动、裁切、重叠或难以阅读的按钮。

## TEST 4 - 进度

运行收藏同步，观察“读取收藏”和“更新本地漫画库”阶段。导入 CSV 或数据包，确认显示读取文件、解析校验、写入和完成阶段。导出 Browser Lite 时确认阶段会随真实操作变化。

## TEST 5 - 下载

下载一个已知章节。在完成前确认任务显示漫画标题、章节标题、已下载图片 / 总数、百分比、字节数和速度；只有预期字节已知时才显示 ETA。暂停并恢复，确认进度保留；取消前应能看到当前进度。

## TEST 6 - 下载命名

确认任务主标题是漫画名，第二行是章节名。不得把 Provider ID、内部任务 ID 或文件系统 slug 作为主标题。

## TEST 7 - 已下载

完成至少一张图片后打开“已下载”。确认漫画出现，并显示“部分下载”；全部已知图片存在时显示“完整”。测试网格 / 列表和大 / 中 / 小。

## TEST 8 - 封面

在漫画库、推荐和已下载分别关闭封面并滚动，确认只显示占位图且没有新增封面请求；重新打开后确认图片懒加载。

完成后关闭 Pica Library，确认没有残留进程或端口。记录失效页面、错误计数、泄露凭据或下载状态不更新的复现步骤。
